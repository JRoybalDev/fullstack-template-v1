{
  "name": "fullstack-template-v1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "typecheck": "bun run typecheck:all",
    "typecheck:all": "bun --cwd packages/schema run typecheck && bun --cwd apps/server run typecheck && bun --cwd apps/web run typecheck",
    "build": "bun run build:all",
    "build:all": "bun --cwd packages/schema run build && bun --cwd apps/server run build && bun --cwd apps/web run build",
    "db:up": "bun run scripts/compose.ts up -d db",
    "db:down": "bun run scripts/compose.ts down",
    "db:logs": "bun run scripts/compose.ts logs -f db"
  },
  "devDependencies": {
    "@types/bun": "^1.3.3",
    "typescript": "^5.9.3"
  }
}
